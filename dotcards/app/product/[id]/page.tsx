import React, { FC } from "react";

interface Props {
  params: {
    id: string;
  };
  searchParams: any;
}

const ProductPage: FC<Props> = (props: Props) => {
  return <main></main>;
};

export default ProductPage;
